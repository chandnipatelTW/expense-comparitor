allprojects {
    repositories {
        mavenCentral()
    }
}

subprojects {
    apply plugin: 'java'
}

project(':expense_matcher_cli') {
    dependencies {
        compile project(':expense_matcher')
    }
}

